kind = "web"
previewPath = "/"
title = "FlowMind"
version = "1.0.0"
id = "artifacts/flowmind"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22473

[services.development]
run = "pnpm --filter @workspace/flowmind run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/flowmind", "run", "build" ]
publicDir = "artifacts/flowmind/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22473"
BASE_PATH = "/"
